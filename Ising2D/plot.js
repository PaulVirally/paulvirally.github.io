class Plot {
    draw(sketch, tempVals, fieldVaps, magVals) {
        sketch.clear(); // Clear out the previous plot

        //
    }

    #plotArray(arr) {
        arr.forEach((y, i) => {
            const x = i * 10; // TODO: Convert index space to x-axis space
            // TODO: nope, we need (x1, y1) and (x2, y2)
        });
    }
}
